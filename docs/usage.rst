========
Usage
========

To use djangocms-responsive-wrapper in a project::

1. Add `responsive_wrapper` to `INSTALLED_APPS`::

    INSTALLED_APPS = (
        ...
        'responsive_wrapper',
        ...
    )
   